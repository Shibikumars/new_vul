name: Security Check

on:
  pull_request:
    branches: [ main ]

jobs:
  vulnerability-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          pip install fastapi uvicorn
          # Add your security tool install here (e.g., bandit, or your own script)
      - name: Run Vulnerability Check
        run: |
          # Example: run your code_review.py or other script here
          echo "Run your vulnerability detector here"